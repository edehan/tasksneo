# TaskFlow Landing

独立部署的品牌落地页（Vite + TypeScript）。

## 本地开发

```bash
pnpm --filter landing dev
```

## 构建

```bash
pnpm --filter landing build
```

## 环境变量

复制 `.env.example` 为 `.env`：

- `VITE_APP_LOGIN_URL`: 主应用登录页地址
- `VITE_APP_REGISTER_URL`: 主应用注册页地址
