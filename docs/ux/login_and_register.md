## 进入注册页

**触发**：用户访问系统，无 Token，被重定向到 `/login`。点击"没有账号？注册"。

**页面**：`/register`

**用户看到**：

- 一个居中的卡片，卡片宽度约 400px，垂直居中于页面
- 卡片标题："创建账号" - 注册关闭时 → 页面整体显示"注册功能当前未开放"提示，不显示表单

第一步：只要求输入邮箱 `type="email"`，点击确认按钮

- "已有账号？登录"链接

一封验证邮件已发送到你的邮箱，点击链接继续注册（失败情况：邮箱已被注册）
未收到？重新发送（在后端进行计数每24小时最多5封以避免spam）

用户点击链接完成验证，进入正式注册页面：

- 表单字段（从上到下）：
  1. 邮箱（已自动填写不可更改）
  2. 密码（必填，`type="password"`，placeholder 提示"至少 8 位"）
  3. 昵称（选填，placeholder "留空则使用邮箱展示"）
  4. 学校（选填，下拉选择，数据来自 `GET /schools`，第一项为"不填写"）
  5. 学号（条件必填：选择学校后此字段变为必填，且出现红色星号）
- 复选框："我同意《用户协议》和《隐私政策》"（链接指向静态页面，均在新标签页打开）内容请随便写一点占位
- "注册"按钮（Primary，全宽）

**交互细节**：
- 选择学校后，学号字段从 disabled/灰色变为 enabled，label 旁出现红色 `*`
- 清除学校选择后，学号字段重新变为 disabled
- 点击注册 → 按钮变为 loading 状态（禁用 + spinner）
- 成功 → 自动登录，跳转到 `/dashboard`，同时 Toast 提示"欢迎！你的个人空间已创建"
- 失败（学号冲突）→ 学号字段下出现红色提示"此学校下该学号已存在"


完成后进入dashboard：- enter_dashboard.md

## 登录页面
增加：忘记密码流程。重置密码的链接发送到邮箱 （在后端进行计数每24小时最多5封以避免spam）
重置密码的页面包含：新密码输入框，确认新密码输入框，提交按钮。提交后提示密码重置成功，并自动登录回到主页


