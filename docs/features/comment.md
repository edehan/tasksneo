评论功能，该功能只展示在任务详情页面。

具体的页面布局调整如下：
1. 附件区域调整：
   把全部下载的按钮去掉吧。然后附件如果多了考虑自动折叠起来。
2. 评论区域设计：
   (a) 在附件区域下方增加评论区。
   (b) 评论按时间顺序排列，时间越早的排在越上面。
   (c) 显示每个人的名称和头像，并提供回复按钮。

交互与通知逻辑：
1. 回复功能：点击回复后，内容显示为“@某人 + 回复内容”。
2. 邮件系统对接：
   (a) 如果用户是直接对任务进行评论（而非回复某人），则将该评论抄送给班级里的每个管理员。
   (b) 如果用户是针对某人进行的回复，则将提醒通知发送到被回复人的电子邮箱里。